# DEPRECATED for BoxAI production application deploys.
#
# Production architecture (canonical):
#   - App: native binary + systemd  →  see deploy/boxai2.service
#   - Postgres + Redis: Docker only →  deploy/docker-compose.infra.yml
#   - Deploy: make deploy / scripts/deploy-prod.sh
#   - Docs: deploy/README.md
#
# This file is kept only as a historical pointer so accidental
# `docker compose up` does not look like a supported BoxAI path.
# Use deploy/docker-compose.infra.yml on the server instead.

services: {}
